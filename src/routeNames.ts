// The string values of this object are used as keys for the that specific route. So keep in mind
// the following:
// * String values cannot repeat between screens. (current structure Safety_[feature]_[screen])

const RouteNames = {
  Welcome: {
    home: 'rnTemplate.Welcome.Home',
    dashboard: 'rntemplate.Welcome.dashboard',
  },
};

export default RouteNames;
